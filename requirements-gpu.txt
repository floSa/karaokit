# Dépendances Python — variante GPU (NVIDIA CUDA).
# Installe torch compilé pour CUDA 12.1. Adapte l'URL si ta version de CUDA diffère
# (voir https://pytorch.org/get-started/locally/).
--extra-index-url https://download.pytorch.org/whl/cu121

torch
torchaudio

# Brique 1 — séparation voix/instrumental
demucs

# Brique 3 (niveau 3) — transcription + forced alignment mot-à-mot
whisperx

# Brique 2 (niveau 2) — alignement forcé d'un texte connu sur la voix.
# Utilise le moteur MMS_FA intégré à torchaudio (déjà installé ci-dessus) :
# multilingue, wheel précompilé, AUCUN compilateur C++ requis. Rien à ajouter ici.

# Brique 2 — récupération des paroles en ligne (LRCLIB, Musixmatch, Genius…)
syncedlyrics
